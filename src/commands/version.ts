import { GluegunToolbox } from "gluegun"

module.exports = {
  name: 'log',
  run: async (toolbox: GluegunToolbox) => {
    const { print } = toolbox
                                            
  }
}
